# GitHub Pages builds this site with the github-pages gem. Declaring it here
# lets CI build the site exactly the way Pages does before running the browser
# regression tests. GitHub Pages itself also reads this file.
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
